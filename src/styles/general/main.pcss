html {
  box-sizing: border-box;
  height: 100%;
}

*,
*::before,
*::after {
  box-sizing: inherit;
}

body {
  min-height: 100%;
}

::placeholder {
  color: map-get($colors, gray-light);
}

input {
  width: 100%;
  color: inherit;
}
